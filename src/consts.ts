/**
 * Layout constants the scroll scripts need at runtime.
 *
 * These are duplicated as CSS custom properties in `styles/tokens.css`; keep
 * the two in step.
 */

/** Navbar height in px — used by the navbar's detach threshold. */
export const NAVBAR_HEIGHT = 56;
